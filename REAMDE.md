# SportFit — Программа тренировок на 30 дней

Адаптивный лендинг для фитнес-платформы. Проект выполнен с использованием современной методологии верстки и кастомного JavaScript-слайдера.

## 🚀 Особенности проекта

* **Fluid Typography & Layout**: Использование миксины `fluid-prop` для плавной адаптации размеров (шрифты, отступы, ширина контейнеров) без резких скачков.
* **Custom Slider**: Самописный слайдер на чистом JavaScript (Vanilla JS):
    * Поддержка разного количества слайдов на брейкпоинтах: 3 (Desktop), 2 (Tablet), 1 (Mobile).
    * Центрирование контентной области при любом количестве видимых карточек.
    * Поддержка тач-событий (swipe) для мобильных устройств.
    * Умная навигация: блокировка стрелок в крайних положениях и пагинация (dots).
* **Marketing Ready**: Интеграция с Facebook Pixel для отслеживания конверсий (событие Lead).
* **Performance**: Использование современных форматов изображений (`.webp`) и тега `<picture>` для оптимизации загрузки на разных устройствах.

## 🛠 Технологический стек

* **HTML5**: Семантическая верстка.
* **SCSS (Sass)**:
    * Архитектура 7-1 (разделение на base, components, sections, layout).
    * Методология BEM (Block Element Modifier).
    * Использование `@use` и `@forward` вместо устаревшего `@import`.
* **JavaScript**: ES6+, модульный подход (разделение логики слайдера и аналитики).

## 📂 Структура проекта

* `scss/base/` — переменные, миксины и сброс стилей.
* `scss/components/` — стили кнопок, слайдера и мелких элементов.
* `scss/sections/` — стили для конкретных блоков (Hero, Program).
* `js/slider.js` — основная логика слайдера.
* `js/pixel.js` — обработка маркетинговых событий.

## 📈 Маркетинговая интеграция (Facebook Pixel)

В проекте реализовано отслеживание лидов. При клике на основную кнопку призыва к действию (CTA) в блоке Hero, отправляется событие `Lead`.

```javascript
// Пример вызова в pixel.js
fbq('track', 'Lead');